#include <stdio.h>

typedef struct() {
    char adressEtc[];
} iDontUnderstandPointers;

int main() {
    float height = 92.0;
    printf("%f", height);
    if (height > 90) {
        printf("Allowed");
    } else {
        printf("Sorry");
    }
    return 0;
}
